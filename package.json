{
  "name": "@client-platform/cross-platform",
  "version": "0.0.0",
  "description": "Cross-platform CLI for Client Platform Labs",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "cross-platform": "./bin/cross-platform.js"
  },
  "exports": {
    ".": {
      "types": "./dist/cli.d.ts",
      "import": "./dist/cli.js"
    }
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cross-platform": "node ./bin/cross-platform.js"
  },
  "dependencies": {
    "@client-platform/kernel": "file:../kernel/packages/kernel",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "clientPlatform": {
    "name": "@client-platform/cross-platform",
    "schemaVersion": "1",
    "compatibility": {
      "cli": ">=0.0.0",
      "manifest": ">=1"
    },
    "supports": {
      "targets": ["h5"]
    },
    "capabilities": ["commands"]
  }
}
